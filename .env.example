# =================================
# == API Base URL Configuration ==
# =================================

# Host where the backend API is running
# Example: 'http://localhost' for local development
VITE_API_HOST=http://localhost

# Port where the backend server is listening
# Example: '4040' for default Spring Boot server
VITE_API_PORT=4040

# Base path of the backend API
# Example: '/api' to prefix all API endpoints
VITE_API_BASE_PATH=/api

# Full base URL for backend API (joined manually in code from above parts)
# Example: http://localhost:4040/api
# ⚠️ Vite does not support variable substitution in .env, so this must be built in code
# Do NOT rely on this key directly if you're already using the 3 variables above
VITE_API_BASE_URL=http://localhost:4040/api

# =======================
# == JWT Configuration ==
# =======================

# This value is NOT used directly — token should be stored in localStorage after login
# You can optionally use this as a default or fallback (not recommended for production)
VITE_JWT_TOKEN=YOUR_JWT_TOKEN_HERE
